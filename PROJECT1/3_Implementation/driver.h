#include "MyStm32f407xx.h"
#include<stdio.h>
#define "MAX 5"
#ifndef "MAX 5"

#define BTN_PRESSED ENABLE
// enabaling the button press
void lock();
// shows lock when function is called
void unlock();
// shows unlock when the function is called
void alarm ON/OFF ();
// shows alarm ON/OFF when the function isa called
void Car approach ON();
// shows car approach ON when the function is called
void delay_1(void);
// provides required delay
int button_count(void);       // Providing Count Value

#endif
