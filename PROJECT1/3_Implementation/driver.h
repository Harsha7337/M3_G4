#include "MyStm32f407xx.h"
#include<stdio.h>
#define "MAX 5"
#ifndef "MAX 5"

#define BTN_PRESSED ENABLE
// enabaling the button press
void windowstatus();
// shows window status when function is called
void alarmststus();
// shows alarmstatus when the function is called
void batteryinfo();
// shows battery info when the function isa called
void doorstatus();
// shows door status when the function is called
void delay_1(void);
// provides required delay
int button_count(void);       // Providing Count Value

#endif
